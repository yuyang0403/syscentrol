zuul:
  routes:
    user-manager:
      path: /user-manager/**
      serviceId: micservice-user
    user-api:
      path: /user-api/**
      serviceId: micservice-user
    sys-user:
      path: /user-login/**
      serviceId: micservice-user
ribbon:
  ReadTimeout: 60000  # 单位毫秒数
  SocketTimeout: 60000
#logging.level.ROOT: DEBUG
